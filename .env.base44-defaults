# Local placeholder defaults — real secrets are delivered via /run/base44/app.env
# and always override these (env_file order: this file first, app.env last).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
